{
  "name": "07-static-web-apps",
  "version": "1.0.0",
  "description": "Azure Static Web Apps example with frontend and API",
  "type": "module",
  "scripts": {
    "setup": "./setup.sh",
    "dev": "npx swa start frontend --api-location api",
    "build:api": "cd api && npm install && npm run build",
    "build": "npm run build:api"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.6"
  }
}
