{
  "name": "08-kubernetes",
  "version": "1.0.0",
  "description": "Azure Kubernetes Service (AKS) example",
  "type": "module",
  "scripts": {
    "setup": "./setup.sh",
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "npx tsx src/server.ts",
    "docker:build": "docker build -t aks-demo-app .",
    "docker:run": "docker run -p 3000:3000 aks-demo-app",
    "deploy": "./deploy.sh"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2",
    "tsx": "^4.6.2"
  }
}
